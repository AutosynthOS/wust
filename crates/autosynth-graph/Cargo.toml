[package]
name = "autosynth-graph"
version = "0.1.0"
edition = "2024"

[dependencies]
autosynth-isa = { path = "../autosynth-isa" }
smallvec = "1"
slotmap = "1"
wust-core = { path = "../wust-core" }

[dev-dependencies]
wat = "1.245.1"
