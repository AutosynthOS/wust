[package]
name = "autosynth-ir"
version = "0.1.0"
edition = "2024"

[features]
trace = ["serde", "autosynth-isa/trace"]

[dependencies]
autosynth-isa = { path = "../autosynth-isa" }
serde = { version = "1", default-features = false, features = ["derive"], optional = true }
