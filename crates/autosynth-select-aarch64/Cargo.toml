[package]
name = "autosynth-select-aarch64"
version = "0.1.0"
edition = "2024"

[dependencies]
autosynth-ir = { path = "../autosynth-ir" }
autosynth-isa = { path = "../autosynth-isa" }
autosynth-isa-aarch64 = { path = "../autosynth-isa-aarch64" }
